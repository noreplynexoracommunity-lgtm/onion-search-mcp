[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Zabij stary kontener PRZED startem nowego -- na free tier ngrok nie da
# postawic drugiego tunelu na tym samym zarezerwowanym domenie.
overlapSeconds = 0
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
