# DragonFF (opcjonalne, dla MTA SA .dff/.txd/.col)

Wrzuć tutaj `DragonFF.zip` z https://github.com/Parik27/DragonFF
(Releases → najnowszy zip).

Kontener przy starcie sam go rozpakuje do addonów Blendera i włączy.
Bez tego `export_dff` robi fallback do `.obj`.
